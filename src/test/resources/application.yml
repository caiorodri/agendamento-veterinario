server:
  port: 8080
  servlet:
    context-path: /agendamento-veterinario
  error:
    whitelabel:
      enabled: false

spring:
  application:
    name: agendamento-veterinario-test

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: teste@teste.com
    password: password-falso-para-teste
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  security:
    enabled: false

jwt:
  secret: minha-chave-secreta-muito-longa-apenas-para-testes-1234567890
  expiration: 7200000

endereco: "Rua Falsa de Testes, 123 - Cidade Mock"
contato: "(11) 90000-0000"